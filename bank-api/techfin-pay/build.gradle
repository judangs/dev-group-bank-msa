ext {
    runnable = true
    mainClassEntryPoint = 'org.bank.user.PayApplication'

    archiveBaseName = 'external-pay-api'
    version = '1.0.0'
    type = 'boot'
}

dependencies {
    implementation(project(':bank-application:techfin-pay-application'))
}